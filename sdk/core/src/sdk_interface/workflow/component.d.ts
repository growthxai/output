/**
 * Extract the name from a Outputai component
 * @param fn
 * @returns
 */
export declare function getComponentName( fn: Function ): string | undefined;

/**
 * Check if a function is an Outputai component
 * @param fn
 */
export declare function isComponent( fn: Function ): boolean;
