// Minimal types so TS modules (credentials) can import this JS file
export declare const Storage: {
  load: () => { activityInfo: { workflowType: string }; workflowFilename: string } | undefined;
};
