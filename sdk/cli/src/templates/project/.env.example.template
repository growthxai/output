# Docker Compose service name for development environment
DOCKER_SERVICE_NAME={{projectName}}

# LLM provider keys — resolved from encrypted credentials at worker startup.
# To use a different value, replace with the actual key.
ANTHROPIC_API_KEY=credential:anthropic.api_key
OPENAI_API_KEY=credential:openai.api_key

