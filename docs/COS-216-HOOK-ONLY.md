# COS-216 — Hook-only usage tracking (output framework)

This branch is part of the cross-repo COS-216 effort. Canonical planning lives in atlas at:

`docs/plans/os-v1/agents_hq_final_hook_only/tasks/overview.md`

## Tickets landing on this branch

- `output_01` — forward `runId` on every `emitEvent` payload
- `output_02` — emit `http:request` event for every HTTP call (paid or not)
- `output_03` — changeset + dev release

See the matching files under `tasks/todo/` in the atlas planning tree for scope, acceptance criteria, and verification.
